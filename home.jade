doctype
head
        title Red Bird Chat
        link(rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")
        script(src="/socket.io/socket.io.js")
        script(src="script.js")
        meta(name='viewport', content='width=device-width, initial-scale=1')
        script(src='http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js')
body
    div.container-fluid.text-center
      div.row.page-header
        div.col-sm-12
          h1.text-primary Four Minute Friends
      div.row
        div.col-sm-12
      div.row
        div.col-sm-12
      div.row.jumbotron
        div.col-sm-12
          div.well.well-lg
            div.row
              div.col-sm-3
              #disappear
                div.col-sm-3
                  input#nameTextField.form-group(type='text', class="form-control",value='what\'s your name?', onkeydown='limitText(this.form-group#nameTextField,this.form-group.countdown,15);', onkeyup='limitText(this.form-group#nameTextField,this.form-group#nameTextField.countdown,15);', maxlength='15')
                div.col-sm-1
                  button#nameBut.btn.btn-primary(onclick='setName()') Set Name
              div.col-sm-5
          div.col-sm-8
            input#chatTextField.form-group(type='text', class="form-control",value='write your text here...',onkeydown='limitText(this.form-group#chatTextField,this.form-group.countdown,180)', 'currentCount(-1)', onkeyup='limitText(this.form-group#chatTextField,this.form-group#chatTextField.countdown,180)', maxlength='180')
            font(size='1')
            | (Maximum characters: 180)
          div.col-sm-1
            button.btn.btn-primary(onclick='sendMessage()') Send
          div.col-sm-3
      div.row
        div.col-sm-12.bottom

    script.
          function setName() {
          var x = document.getElementById("nameTextField");
          var dis = document.getElementById("disappear");
          var unblock = document.getElementById("chatTextField");

          name = x.value; // <--- USE THIS FOR BACKEND

          unblock.value = "write your text here..."
          x.value = " "
          dis.style.display = "none"
          dis.style.visibility = "hidden"
          }
          function sendMessage() {
          var x = document.getElementById("chatTextField");

          var messageString = x.value // <--- USE THIS FOR BACKEND

          x.value = " "
          // function(name,messageString) USE THIS
          }
          function limitText(limitField, limitCount, limitNum) {
          if (limitField.value.length > limitNum) {
          limitField.value = limitField.value.substring(0, limitNum);
          } else {
          limitCount.value = limitNum - limitField.value.length;
          }
          }
